## Release v1.7.8

**Release Date:** July 27, 2026

Hotfix **v1.7.8** относительно **v1.7.7**: дубликат ключа i18n ломал старт panel.

**Миграция БД:** нет.

---

### Bug Fixes

- **Panel crash loop on start** (`web/translation/translate.en_US.toml`)
  - Удалён дубликат `amneziawgResetDefaults` (ошибка `toml: key amneziawgResetDefaults is already defined`).

### Operational Notes

- Обновите только **sharx** (переводы в образе panel). **sharxnode** менять не обязательно.

### Test plan

- [ ] Panel стартует без restart loop
- [ ] Лог без `key amneziawgResetDefaults is already defined`

### 🐳 Docker Images

Images are available in Harbor:

- **sharx**: {{SHARX_TAG_VERSION}} / {{SHARX_TAG_LATEST}}
- **sharxnode**: {{SHARXNODE_TAG_VERSION}} / {{SHARXNODE_TAG_LATEST}}
- **postgres**: {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:16-alpine / {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:latest

### 📦 Upgrade from v1.7.7

```yaml
services:
  sharx:
    image: {{SHARX_TAG_LATEST}}
```

### Changes (git)

- Сводка: `git diff v1.7.7 v1.7.8 --stat`
- Коммиты: `git log v1.7.7..v1.7.8 --oneline`
